// Preview the REAL planner page (Progress tab) against mock data — used to
// verify the done-count override and click-to-toggle course chips.
import './chromeStub';
import '../src/planner/planner.css';
import '../src/planner/main';
