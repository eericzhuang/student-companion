# Copy to .env and fill in. NEVER commit .env.
STRIPE_SECRET_KEY=sk_test_...
ANTHROPIC_API_KEY=sk-ant-...
# Comma-separated owner tokens; each acts as a permanent Supreme license.
# Generate one: node -e "console.log('adm_'+crypto.randomUUID().replaceAll('-',''))"
ADMIN_TOKENS=
BASE_URL=http://localhost:8787
PORT=8787
