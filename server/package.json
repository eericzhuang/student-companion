{
  "name": "wsc-billing-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Billing backend for Student Companion for Workday: Stripe Checkout + subscription verification",
  "scripts": {
    "setup": "node --env-file=.env setup-products.js",
    "start": "node --env-file=.env index.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "stripe": "^17.7.0"
  }
}
